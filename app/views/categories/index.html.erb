<article class="featured-article d-flex flex-column j-content-end" style ="background: no-repeat top center/cover url('<%= rails_blob_url(@featured_article.picture) %>');">
  <div class="article-content">
    <h2><%=link_to(@featured_article.title,article_path(@featured_article.id),class: "article-title")%></h2>
    <p><%=@featured_article.text.truncate(100)%></p>
  </div>
  
 
</article>
  <%= show_categories(@categories) %>
